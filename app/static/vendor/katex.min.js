window.katex = {
    render: function(element, target, options) {
        target.innerHTML = `<span style="font-style: italic; font-family: math; color: #93c5fd;">[Math: ${options.throwOnError ? '' : (element || '')}]</span>`;
    }
};
